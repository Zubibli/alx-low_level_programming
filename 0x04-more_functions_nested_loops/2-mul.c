#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * mul - takes a two parameter
 * @a: parameter
 * @b: parameter
 * Description: This function takes a parameter @a
 * and @b then multiply
 * Return: 0
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
