#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _iupper - a function that checks uppercase
 * @c: character to check
 * Return: 1 success
 */

int _isupper(int c)
{
	if isupper(c)
		return (1);
	else
		return (0);
}
