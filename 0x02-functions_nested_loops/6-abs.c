#include <stdlib.h>
#include "main.h"

/**
 * _abs - this function
 * Description: takes the parameter and return the absolute of it
 * @n: parameter to return absolute
 * Return: returns the absolute of the parameter
 */

int _abs(int n)
{
	int absolute = abs(n);

	return (absolute);
}
